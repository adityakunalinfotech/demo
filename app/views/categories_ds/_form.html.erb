<%= form_for(@categories_d) do |f| %>
  <% if @categories_d.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categories_d.errors.count, "error") %> prohibited this categories_d from being saved:</h2>

      <ul>
      <% @categories_d.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label      :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
